{
  "name": "tweedr",
  "version": "1.0.0",
  "description": "backend for tweedr app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "node server.js",
    "debugger": "DEBUG=*:* npm run dev",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+git@git.generalassemb.ly:aileenmwong/team-tweeder-hw.git"
  },
  "keywords": [
    "tweedr",
    "express",
    "react"
  ],
  "author": "wdi ada",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.1",
    "cookie-parser": "^1.4.3",
    "cors": "^2.8.4",
    "express": "^4.15.2",
    "morgan": "^1.8.2",
    "pg-promise": "^5.9.7"
  },
  "engines": {
    "node": "8.5.0",
    "npm": "5.3.0"
  }
}
